Value LOCAL_INTERFACE (\S+)
Value CHASSIS_ID (\S+)
Value NEIGHBOR_PORT_ID (\S+)
Value NEIGHBOR_NAME (\S*)
Value CAPABILITIES (\S+(?:\s*,\s*\S+)*)
Value TTL (\d+)

Start
  # 跳过表头和分隔线
  ^System\s+capability -> Continue
  ^[BRWTDHOPS] -> Continue
  ^\s+Port\s+Device -> Continue
  ^-+ -> Continue
  ^\s*$$ -> Continue
  # 匹配数据行
  ^${LOCAL_INTERFACE}\s+${CHASSIS_ID}\s+${NEIGHBOR_PORT_ID}\s+${NEIGHBOR_NAME}\s+${CAPABILITIES}\s+${TTL}\s*$$ -> Record

EOF
